import 'package:flutter/material.dart';

class PokemonListFailure extends StatelessWidget {
  const PokemonListFailure({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Error'));
  }
}
